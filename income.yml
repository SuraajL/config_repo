server:
  port: 8083
  error:
    include-message: always

spring:
  application:
    name: income
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: taxCalc
      port: 27017
      host: hello
  rabbitmq:
    addresses: rabbitmq:5672
