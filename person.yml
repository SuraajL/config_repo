server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true

spring:
  application:
    name: person
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: taxCalc
      port: 27017
      host: mongodb
      auto-index-creation: true
      uri: mongodb://rootuser:rootpass@mongodb:27017/taxCalc
